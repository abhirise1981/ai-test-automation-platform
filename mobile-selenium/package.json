{
  "name": "toptal-mobile-selenium",
  "private": true,
  "description": "Mobile-flavour test harness using Selenium WebDriver 4 + Chrome DevTools mobile emulation. Separate from the Playwright suite by design - see README.md for why.",
  "scripts": {
    "test": "mocha -r ts-node/register tests/**/*.test.ts --timeout 60000"
  },
  "devDependencies": {
    "selenium-webdriver": "^4.27.0",
    "@types/selenium-webdriver": "^4.1.28",
    "mocha": "^11.0.1",
    "@types/mocha": "^10.0.10",
    "chai": "^5.1.2",
    "@types/chai": "^5.0.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  }
}
